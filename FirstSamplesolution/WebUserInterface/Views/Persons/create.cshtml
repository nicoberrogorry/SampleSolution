
@model WebUserInterface.Models.PersonViewModel

@{
    ViewBag.Title = "create";
}

@using (Html.BeginForm("Create", "Persons", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.DropDownListFor(@model => @model.SelectedProfession, new SelectList(Model.Professions, "ProfessionId", "Description", Model.SelectedProfession, "-- Select --"))

    <input type="submit" value="Confirm" class="button button-3d nomargin" />
}